"""The theme boundary and the browser: contracts, windows, options, input.

Not named `frontend`: a test folder is a top-level package during discovery, so that
name would shadow the package it tests."""
